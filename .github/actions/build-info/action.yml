name: build-info
description: print branch,commit and python version
runs:
  using: composite
  steps:
    - run: echo "Branch $GITHUB_REF_NAME"
      shell: bash
    - run: echo "Commit $GITHUB_SHA"
      shell: bash
    - run: python --version
      shell: bash
