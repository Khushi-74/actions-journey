name: github variables
on:
  push:
    branches-ignore:
      - main
jobs:
  myjob:
    runs-on: ubuntu-latest
    steps:
      - name: print the deployed region
        run: echo "${{ vars.DEPLOY_REGION }}"
