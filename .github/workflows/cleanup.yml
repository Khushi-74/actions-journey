name: cleanup
on: push
jobs:
  myjob:
    runs-on: ubuntu-latest
    steps:
      - name: Start test database
        run: echo "start database"
      - name: Flaky check
        run: exit 1
      - name: Stop test database
        if: always()
        run: echo "stop the database"
